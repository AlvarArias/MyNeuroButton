import SwiftUI

public extension Color {
    /// Light grey-blue neumorphic background — sourced from `NeumorphicTheme.default`.
    static let neumorphicBackground = NeumorphicTheme.default.background

    /// Medium grey neumorphic foreground — sourced from `NeumorphicTheme.default`.
    static let neumorphicForeground = NeumorphicTheme.default.foreground
}
